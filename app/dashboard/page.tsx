'use client';
import {useEffect,useState} from 'react';import {supabase} from '../../lib/supabase';
type App={id:string;company:string;job_title:string;status:string;match_score:number|null;created_at:string};
export default function Dashboard(){const[apps,setApps]=useState<App[]>([]);const[email,setEmail]=useState('');const[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){location.href='/auth';return}setEmail(user.email||'');const{data}=await supabase.from('applications').select('id,company,job_title,status,match_score,created_at').order('created_at',{ascending:false});setApps(data||[]);setLoading(false)})()},[]);async function signout(){await supabase.auth.signOut();location.href='/'}return <main className="dash"><nav><a href="/"><b>HIREVORA</b></a><button className="ghost" onClick={signout}>Sign out</button></nav><header><span className="pill">APPLICATION DASHBOARD</span><h1>Your job search.</h1><p>{email}</p><a className="primary" href="/#start">+ New application</a></header><section className="appList">{loading?<p>Loading…</p>:apps.length?apps.map(a=><article key={a.id}><div><small>{a.company||'Company'}</small><h2>{a.job_title||'Untitled application'}</h2></div><div><b>{a.match_score??'—'}{a.match_score!=null?'%':''}</b><span>{a.status}</span></div></article>):<article><div><small>NO APPLICATIONS YET</small><h2>Build your first tailored application.</h2></div><a href="/#start">Start →</a></article>}</section></main>