'use client';
import {FormEvent,useState} from 'react';import {supabase} from '../../lib/supabase';
export default function Auth(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[loading,setLoading]=useState(false);async function go(e:FormEvent,signup=false){e.preventDefault();setLoading(true);setMsg('');const r=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});setLoading(false);if(r.error)return setMsg(r.error.message);if(signup&&!r.data.session)return setMsg('Check your email to confirm your account.');location.href='/dashboard'}return <main className="auth"><a href="/">← HIREVORA</a><form onSubmit={e=>go(e)}><span className="pill">YOUR CAREER WORKSPACE</span><h1>Welcome to Hirevora.</h1><p>Sign in to save applications and keep your job search organized.</p><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={loading}>{loading?'Working…':'Sign in'}</button><button className="ghost" type="button" disabled={loading} onClick={e=>go(e as any,true)}>Create account</button>{msg&&<p>{msg}</p>}</form></main>